Place any images referenced by absolute paths here so they are served from the site root (e.g. /Logo.jpeg).

Required filenames used by the app (add your image files with these exact names):
- Logo.jpeg
- signupprofilepic.png
- lock.png
- linkdin.png
- twiter.png
- logo.png

Add other images here if referenced by absolute paths.
